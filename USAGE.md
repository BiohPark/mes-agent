# MES Agent 사용 가이드

사내 폐쇄망 환경에서 자연어로 업무를 자동화하는 AI 데스크탑 에이전트입니다.

---

## 빠른 시작

### 1. 앱 실행

```powershell
# 콘다 환경 활성화 및 앱 시작
.\start.ps1
```

Electron 앱 창이 열리고 하단 상태 표시줄에 **"준비됨"** 이 표시되면 사용 가능합니다.

### 2. 첫 대화

입력창에 자연어로 작업을 입력하고 **Enter** 키를 누릅니다.

```
예) "Syncade에서 생산 라인 현황 스크린샷 찍어줘"
     "작업지시서 Excel 파일 열어서 5번 행에 오늘 날짜 넣어줘"
     "크롬으로 사내 포털 열어서 로그인해줘"
```

---

## 화면 구성

```
┌──────────────────────────────────────────────────────────────────┐
│  MES Agent     [컨텍스트 ████░ 40k/128k]   [모델 선택] ● 준비됨   │ ← 헤더
├──────────────┬───────────────────────────┬────────────────────────┤
│  [업무 목록]  │ 채팅 메시지 영역          │  워크플로우 패널        │
│  💬 기본업무  │                           │  (단계별 진행 현황)     │
│  🚀 Syncade  │                           │                        │
│  🧠 Obsidian │ ────────────────────────  │                        │
│  ...         │ 입력창              [전송] │                        │
└──────────────┴───────────────────────────┴────────────────────────┘
```

### 좌측 사이드바 — 업무 유형

| 아이콘 | 업무 유형 | 주요 용도 |
|--------|----------|----------|
| 💬 | 기본업무 | 일반 자동화 작업 |
| 🚀 | Syncade | MES 시스템 조작 |
| 🧠 | Obsidian | 지식 베이스 관리 |
| 🤖 | Unscript | 스크립트 자동화 |
| 📥 | Knox | 문서 관리 |

업무 유형을 클릭하면 해당 컨텍스트와 시스템 프롬프트로 대화가 시작됩니다.

---

## 주요 기능

### 화면 자동화

에이전트가 화면을 직접 보고 마우스·키보드를 제어합니다.

- **OCR**: 화면에서 텍스트를 읽음
- **이미지 매칭**: 버튼이나 요소를 찾아 클릭
- **스크린샷**: 현재 상태 캡처

```
예) "화면에서 '승인' 버튼 찾아서 클릭해줘"
     "지금 화면 OCR로 읽어줘"
```

### 브라우저 자동화

Chromium 브라우저를 제어합니다.

```
예) "사내 포털 열어서 로그인해줘"
     "현재 열린 페이지에서 폼 작성해줘"
```

> **사전 조건**: `python -m playwright install chromium` (최초 1회 실행)

### 문서 처리

Excel, Word, PDF 파일을 읽고 편집합니다.

```
예) "D:/작업/생산현황.xlsx 열어서 B열 합계 알려줘"
     "보고서.docx에 오늘 날짜 추가해줘"
```

### 워크플로우

복잡한 다단계 작업은 우측 패널에 단계별로 표시됩니다.
각 단계의 상태(진행중/완료/오류)를 실시간으로 확인할 수 있습니다.

---

## 입력 단축키

| 키 | 동작 |
|----|------|
| `Enter` | 메시지 전송 |
| `Shift + Enter` | 줄바꿈 |
| `Ctrl + Enter` | 줄바꿈 (또는 팝업 에디터에서 전송) |
| `⛶` 버튼 | 팝업 에디터 열기 (장문 입력용) |

---

## 에이전트 제어

### 작업 중단

에이전트가 실행 중일 때 **[■ 중단]** 버튼을 클릭하면 현재 작업을 멈춥니다.

### 위험 작업 확인 팝업

파일 삭제, 시스템 명령 등 위험한 작업은 실행 전에 확인 팝업이 표시됩니다.

- **예**: 이번 한 번만 허용
- **항상**: 세션 동안 자동 허용
- **아니오**: 거부

### 계획 모드

헤더의 📋 버튼으로 **계획 모드**를 활성화하면 에이전트가 먼저 작업 계획을 제시하고,
승인 후 실행합니다.

---

## LLM 프로파일 전환

헤더 우측의 드롭다운에서 사용할 LLM과 모델을 선택할 수 있습니다.

- **사내 LLM**: 폐쇄망 내부 모델 사용
- **OpenAI**: 외부 OpenAI API 사용 (외부망 필요)

> 설정은 `.env` 파일의 `LLM_ACTIVE`, `LLM_*_MODEL` 항목으로 기본값을 변경할 수 있습니다.

---

## 장기 기억

에이전트는 스레드가 종료될 때 중요한 정보(선호·결정·사실)를 자동으로 기억하고,
다음 대화에서 관련 기억을 자동으로 참조합니다.

헤더의 **🧠 기억** 버튼으로 저장된 기억을 보고 관리할 수 있습니다.

---

## 자주 묻는 질문

**Q. 에이전트가 응답하지 않아요.**  
A. 하단 상태 표시줄이 "준비됨"인지 확인하세요. "서버 시작 중..."이면 잠시 기다립니다.

**Q. 브라우저가 안 열려요.**  
A. `python -m playwright install chromium`을 실행했는지 확인하세요.

**Q. 한글 입력이 깨져요.**  
A. `type_text_clipboard` 도구를 쓰도록 에이전트에 안내하거나, 입력창에 직접 한글을 입력 후 전송하세요.

**Q. 설치 환경을 재설정하고 싶어요.**  
A. [SETUP.md](SETUP.md)를 참조하세요.

---

## 개발자 도구

사이드바 하단의 **🛠️ 개발자 도구** 접이식 메뉴에서:
- **📷 빠른 OCR**: 화면 전체를 즉시 OCR
- **📂 파일 열기**: 파일 경로를 입력해 처리
- **🔧 도구 테스트**: LLM 없이 개별 도구를 직접 테스트
